-- Nanuk — tus apps por defecto. Se carga ANTES de los bindings, así que con
-- cambiar aquí una línea todos los atajos usan la app nueva.
-- Los defaults están en ~/.config/nanuk/default/hypr/helpers.lua (n.apps).

-- n.apps.terminal = "foot"       -- si instalas otra terminal
-- n.apps.browser = "brave"
-- n.apps.editor = "code"
-- n.apps.files = "nautilus --new-window"
