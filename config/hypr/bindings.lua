-- Nanuk — atajos de teclado, agrupados por tema.
-- Para cambiar uno en tu capa: n.rebind("SUPER + RETURN", "Terminal", "kitty")
-- Para quitar uno:             hl.unbind("SUPER + RETURN")

require("default.hypr.bindings.tiling")      -- ventanas y workspaces
require("default.hypr.bindings.apps")        -- terminal, launcher, browser...
require("default.hypr.bindings.media")       -- volumen, brillo, reproducción
require("default.hypr.bindings.utilities")   -- capturas, portapapeles, lock, power
