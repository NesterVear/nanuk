# Nanuk — apps personales. NO se instalan por defecto.
# Se instalan solo si corres el instalador con NANUK_EXTRAS=1:
#   NANUK_EXTRAS=1 bash install/03-packages.sh
# Los que son del AUR llevan el sufijo ":aur" para que el script sepa usar yay.
keepassxc
obsidian
localsend
libreoffice-fresh
libreoffice-fresh-es
gimp
qbittorrent
rclone
tailscale
typora:aur
dbgate-bin:aur
telegram-desktop-bin:aur

# ── Cortafuegos (opcional) ─────────────────────────────────────────
# Nanuk no trae firewall por defecto (menos es más, y evita el lío
# docker/libvirt + ufw). Si lo quieres, descomenta estas dos líneas y
# reinstala; después:  sudo ufw enable
# ufw
# ufw-docker:aur
